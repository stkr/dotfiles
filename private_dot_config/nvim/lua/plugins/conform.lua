return
{
    'stevearc/conform.nvim',
    lazy = true,
    opts = {
        -- log_level = vim.log.levels.DEBUG,
        default_format_opts = {
            lsp_format = "fallback",
        },
    },
}
